# ===============================
# FastAPI Template Environment Variables
# ===============================

# -----------------------------------
# Application Settings
# -----------------------------------
PROJECT_NAME=FastAPI Template
VERSION=1.0.0
API_V1_STR=/api/v1
ENVIRONMENT=development          # Options: development | production | testing
LOG_LEVEL=info                   # Options: debug | info | warning | error | critical

# -----------------------------------
# JWT Authentication

# -----------------------------------
# Superuser Bootstrap
# -----------------------------------
# Optional: Create a superuser on first run
# Set these to create an initial superuser account
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=change_this_in_prod

# -----------------------------------
SECRET_KEY=your_secret_key_here_change_in_production
ACCESS_TOKEN_EXPIRE_MINUTES=43200
ALGORITHM=HS256

# -----------------------------------
# Database Configuration
# -----------------------------------

# Sync DB URL (used in tests and dev scripts)
DATABASE_URL=postgresql://postgres:dev_password_123@localhost:5432/fastapi_template

# Async DB URL (used in production / async contexts)
ASYNC_DATABASE_URL=postgresql+asyncpg://postgres:dev_password_123@localhost:5432/fastapi_template

# Optional: Test DB URL if separate from main dev DB
TEST_DATABASE_URL=postgresql://postgres:dev_password_123@localhost:5432/fastapi_template_test

# -----------------------------------
# CORS Configuration
# -----------------------------------
# Comma-separated list of allowed origins, or use JSON array format
# Examples:
# BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:4200
# BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://localhost:8080", "http://localhost:4200"]
# Note: JSON arrays in .env files need to be properly quoted
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:4200

# For production, restrict to specific domains:
# BACKEND_CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
